## 1. Architecture and delivery foundation

- [x] 1.1 Record the adopted React + TypeScript + Vite + Astryx + TanStack Query/Table architecture in an ADR, including accessibility baseline, package manager, source directory, asset-size budget, and reproducible build contract. [ADR-0001](../../../webui/docs/adr/0001-local-browser-workspace-frontend.md) records the chosen stack, strict TypeScript, pnpm and frozen lockfile contract, source layout, accessible Astryx baseline, hashed Vite output, and the 250 KiB initial-JavaScript gzip target.
- [x] 1.2 Add a `web` Cobra command and a testable server lifecycle seam without changing default TUI startup behavior. `cli/internal/app/commands_web.go` provides `wechat-article web` and `--no-open`, composes the local server over the selected active profile, emits its one-time loopback URL to stdout, and closes it with the command context. `commands_web_test.go` covers no-open, opener failure, JSON rejection, required runtime seams, and facade injection; `local_commands_test.go` retains no-subcommand interactive Bubble Tea startup.
- [x] 1.3 Create the `internal/web` presentation package and document its allowed dependencies on application/profile runtime seams. `cli/internal/web/doc.go` limits the package to presentation-safe application seams and forbids direct profile, object-store, WeChat-protocol, scheduler, and secret-persistence access; Cobra remains the composition root.
- [x] 1.4 Add deterministic front-end build, hashed asset manifest, `go:embed` packaging, stale-generated-asset CI check, and release build integration. `webui` builds fingerprinted Vite assets and validates them before `sync:go-assets` copies them into `cli/internal/web/assets`; `embed.go` validates the embedded manifest and assets under `//go:embed`. The `webui` CI job performs frozen-lockfile build, stale-asset verification, release-size checking, and both browser E2E lanes, while the downstream release build waits for that job. Focused embedded-asset/header tests preserve immutable fingerprinted assets and `no-store` application shell semantics.
- [ ] 1.5 Establish versioned `/api/v1` response/error/pagination schemas and API compatibility rules. `docs/release/browser-api-contract.md` now documents the exact success/error/page envelope shapes read from `cli/internal/web/api.go`, offset/limit/page/page_size pagination semantics (default 50, maximum 100, either-style-not-both) from `cli/internal/application/workspace.go` and `cli/internal/web/api_query.go`, the closed stable `error.code` vocabulary, the additive-only-within-v1 rule with an explicit breaking-change list, the version-bump/deprecation/support process, and the client rule for ignoring unknown additive response fields. `docs/release/browser-api-route-matrix.md` maps every implemented `/api/v1` route family to its actual Go test evidence (or lack of it) for auth, valid response, invalid input, redaction, and job-ID behavior. `cli/internal/web/api_contract_test.go` adds table-driven tests locking the single-resource, page, error, and job-creation envelope shapes and representative stable error codes; `TestAccountManifestReadAPIIsAuthenticatedGETOnlyAndStreamsBareManifest`, `TestExportListAPIUsesAuthenticatedSafePagedWorkspaceDTO`, and `TestAlbumsAPIRejectsInvalidQueriesBeforeWorkspaceCalls` close the former route-specific coverage gaps. This item remains open solely for exhaustive per-route `error.code` assertions across the documented vocabulary.
- [x] 1.6 Bootstrap Astryx integration with `ThemeProvider`, `LinkProvider`, strict `reset → astryx → theme` CSS layers, generated theme, and `astryx doctor` CI verification. `webui/src/app/App.tsx` initializes theme and link providers before workspace content; `webui/src/styles/index.css` establishes the required CSS layer order; `pnpm run check` generates the theme and runs `astryx doctor` in the release `webui` CI job.

## 2. Loopback security and lifecycle

- [x] 2.1 Bind a random IPv4 loopback port and reject wildcard, LAN, public, malformed Host, and unsupported listener configuration. `Server.Start` binds the fixed `tcp4` address `127.0.0.1:0`, and host validation requires the exact listener address and loopback peer before authorization. `TestWorkspaceBindsOnlyRandomIPv4Loopback` and `TestHostConfusionIsRejectedBeforeBootstrapOrSessionAuthorization` cover random binding and hostile Host forms.
- [x] 2.2 Implement high-entropy in-memory bootstrap token, token-to-HttpOnly-session exchange, URL token removal, session expiry, logout, and shutdown invalidation. The server creates random one-time bootstrap/session/CSRF credentials in memory, exchanges the URL token for scoped cookies and a redirect without the token, expires sessions, clears them on logout, and invalidates them on close. `server_test.go` covers successful/reused/guessed/multiple bootstrap tokens, cookie flags, expiry, logout, and cancellation shutdown.
- [x] 2.3 Implement same-origin Host/Origin/CSRF validation for mutations and request-size/content-type limits. The shared handler validates the exact Host and loopback peer before session use; mutations require the exact listener Origin, CSRF token, supported JSON/multipart shape, and bounded body size. `TestHostOriginCSRFLimitsAndLogout`, multipart upload tests, and maintenance/export mutation tests cover rejected Host, Origin, CSRF, content type, malformed, and oversized inputs.
- [x] 2.4 Add CSP, referrer, no-sniff, frame, cache-control, and error-redaction middleware with header regression tests. `server.go` applies restrictive CSP, `no-referrer`, `nosniff`, frame denial, and no-store headers to stateful responses while fingerprinted assets receive immutable caching. `server_test.go`, `asset_test.go`, preview, manifest, and artifact tests assert the headers; API tests assert generic internal errors do not serialize paths, SQL, secrets, or upstream response bodies.
- [x] 2.5 Implement `--no-open`, browser auto-open, stdout URL/stderr log discipline, signal cancellation, and bounded graceful shutdown. `commands_web.go` emits exactly one local URL to stdout, supports `--no-open`, attempts opening otherwise without leaking the bootstrap URL on failure, and serves until the Cobra context ends. `commands_web_test.go` covers both opener modes and stdout/stderr discipline; `TestSessionExpiryAndCancellationInvalidateCredentials` and lifecycle tests cover shutdown, URL invalidation, and one-shot cleanup.
- [x] 2.6 Add security tests for unauthorized access, guessed/reused token, cross-origin mutation, Host confusion, URL/log leakage, and retired-domain absence. Handler tests reject malformed and conflicting Host forms before bootstrap/session authorization, guessed and repeated/multiple bootstrap tokens without response leakage, and verify exact random IPv4 loopback URLs plus session-cookie issuance/logout clearing. Command tests preserve stdout/stderr URL discipline when browser launch fails and assert normal command output contains no retired project domains; embedded-asset tests reject retired project-domain references.

## 3. Shared browser API

- [x] 3.1 Add runtime/session/status API endpoints backed by the shared application and profile runtime. Authenticated `GET /api/v1/runtime`, `/session`, and `/status` are backed by the shared workspace/application seam and return safe versioned DTOs; `/status` also provides the local CSRF value required by the browser client. `TestReadAPIProvidesVersionedBoundedWorkspaceData`, `TestStatusRequiresSessionAndHasSecurityHeaders`, and cross-adapter tests cover the browser boundary and shared data visibility.
- [ ] 3.2 Add paginated account, article, album, export, job, log, lease, saved-query, storage, and diagnostics read endpoints with bounded responses. The implemented browser API includes bounded job detail, saved-query CRUD, safe article-preview handoff, and maintenance/diagnostics reads; this broad parity item remains open pending the remaining resource coverage.
- [ ] 3.3 Add mutation endpoints for login/logout, discovery, account management, sync, ingestion, download, metadata/comments, album traversal, and job control. Browser evidence now covers discovery-candidate-to-save, an explicit incremental/full synchronization mode, article download/metadata/comments/resource actions, album traversal, bounded multi-album export, and job controls limited to shared-engine `permittedActions`. The shared browser/TUI export selection accepts 1–50 distinct stable local album IDs, resolves and freezes no more than 500 article IDs server-side, preserves album/request and ordinal ordering while deduplicating overlaps, and returns the stable persistent export job ID; handler, TUI, selection-manifest, library-ordering, and sanitized-browser tests cover the implemented handoff. Account-manifest upload/import and Credential JSON upload are covered separately below; this broad item remains open pending the remaining mutation coverage.
- [ ] 3.4 Add export, preview, manifest, verification, opening, preferences, credential, proxy, backup, restore, integrity, garbage-collection, and diagnostic endpoints. Export artifact streaming/opening, account-manifest download/upload/import, Credential JSON upload, preview, maintenance, backup ZIP download, GC, diagnostic-bundle, and single-archive restore upload/prepare/commit endpoints are implemented; this combined parity item remains open pending the remaining endpoint coverage.
- [ ] 3.5 Ensure every long-running endpoint creates or controls a persistent job and returns a stable job ID. The browser API contract classifies bounded synchronous mutations separately from persistent job creation/control; focused handler tests assert `202 Accepted` plus the stable fixture job ID for every implemented job-creation route and the same ID after each implemented job control. This broad item remains open pending the remaining endpoint coverage.
- [x] 3.6 Add event/polling API for QR/login and job state changes with reconnect-safe snapshot semantics. The shared `PollLogin` mutation and authenticated bounded `/api/v1/events/snapshot` polling DTO expose runtime, session, storage, and jobs without a streaming connection; its process-local revision remains stable for unchanged observations and advances only when the sanitized state changes. `TestSnapshotPollingRevisionAdvancesOnlyForObservedStateChanges` proves the revision contract, while browser login and job UI tests consume the polling seams.
- [x] 3.7 Add cross-adapter contract tests comparing browser API outcomes with Cobra/TUI/MCP application seams. `cli/internal/contract/cross_adapter_test.go` compares Cobra, Bubble Tea, stdio MCP, and browser reads over one deterministic application facade. `cli/internal/app/local_browser_workspace_parity_test.go` additionally proves a real temporary profile: Cobra-created accounts are displayed by TUI and browser then durably updated through browser PATCH; an MCP-created job is browser-visible and browser-cancellable through the same runtime.

## 4. File, export, and confirmation safety

- [x] 4.1 Define service-validated export root and descendant directory-handle protocol using configured/default Downloads roots and existing output authorization. The browser now receives an opaque token for the active profile's configured root when present, otherwise the local Downloads fallback; application-level Lstat/OpenRoot/no-symlink/identity checks authorize the selected directory and reject replacement before queuing work.
- [ ] 4.2 Implement streaming download responses for safe generated artifacts and bounded upload staging for account manifests, credentials, backups, and restore archives. Safe generated export artifacts and backup ZIPs stream only through one-shot opaque capabilities; account manifests use bounded private staging, Credentials accept one bounded JSON file directly into the parser, and restore archives have bounded private staging. All three multipart entry points reject raw traversal, Windows/UNC host paths, controls, and filename-parameter normalization tricks before staging or mutation; benign single-file names remain accepted. A backup archive selected for restore uses that existing restore-archive staging workflow rather than a separate backup-upload path; generated backups are one-shot downloads. This combined item remains open pending the remaining complete file-boundary coverage.
- [x] 4.3 Integrate uploaded restore archives with the existing staged transactional restore pipeline and cleanup guarantees.
- [ ] 4.4 Preserve existing exact scoped confirmation behavior for deletion, job cancellation, restore, GC, credential removal, and proxy trust operations. Browser dialogs now require a typed proof for account/saved-query deletion and pause/retry/cancel job actions, keep resume confirmation-free, autofocus the proof field, disable the destructive action until it matches, and return focus after cancellation; typed-dialog semantics are covered by sanitized browser E2E. This broad item remains open pending the complete stated confirmation surface.
- [ ] 4.5 Add path traversal, symlink/escape, oversized upload, archive abuse, failed-restore rollback, and secret non-echo regression tests. Focused tests now cover unsafe multipart filenames across all browser upload endpoints and reject ZIP symlink entries during backup verification and restore before live-state mutation; this broad item remains open pending all listed negative cases.

## 5. Browser workspace user experience

- [x] 5.1 Build the React/Astryx app shell, local connection/error states, profile/session home, local SPA navigation, layout responsiveness, and English/Simplified Chinese resource system. `webui/src/app/App.tsx` composes the Astryx theme/link providers and workspace query client; `Workspace.tsx` supplies local connection/error/loading states, the profile/session/storage home, lazy SPA routes, side navigation, and a locale switch. `src/i18n/` provides typed English and Simplified Chinese catalogs, initializes from the local profile/browser preference, persists changes safely, and updates `html[lang]`; `index.css` reflows the shell and grids below 720px. Sanitized Playwright coverage proves SPA back/forward navigation and focus, narrow export usability, and both immediate/persisted Chinese-language selection.
- [x] 5.1a Configure TanStack Query cache/polling/invalidation and TanStack Table server-side pagination, sorting, columns, and multi-selection without whole-library client loading. `webui/src/lib/queries.ts` gives each safe local resource a stable query key, bounded polling where live state is needed, reconnect invalidation, mutation invalidation, and `keepPreviousData` for server pages. `ArticleTable.tsx` and `ResourceTable.tsx` configure TanStack Table with explicit manual pagination/sorting, column visibility, bounded multi-selection, and page-local API data; [ADR-0001](../../../webui/docs/adr/0001-local-browser-workspace-frontend.md) records the no-whole-library-loading contract. Browser E2E covers selection handoffs across bounded article/album pages and server-side export handoffs.
- [ ] 5.2 Implement QR login/status/logout, account discovery/import/export/manage, and account synchronization views. The browser has account-manifest download plus single-file staged JSON upload/import evidence, and sanitized E2E proves a discovery candidate populates the explicit save form and synchronization can be explicitly incremental or full. This broader workflow remains open pending the remaining account UX parity.
- [ ] 5.3 Implement paginated article grid, compound filters, saved queries, columns, multi-selection, single-URL ingestion, safe preview, local HTML handoff, downloads, comments, metrics, and resource completeness actions. Saved-query CRUD and safe local article preview are implemented; article selections now persist across server pages and hand off stable IDs, while bounded article details expose sanitized metrics and resource availability/detail summaries alongside job-backed article actions. The selected-article detail now reads existing locally stored comments in bounded deterministic pages and keyboard-expandable, server-paginated replies, including empty, unavailable, loading, and pending-reply partial states; it does not initiate upstream comment fetching. This broader resource-completeness item remains open.
- [ ] 5.4 Implement album browsing, ordering/traversal, batch download, and export workflows. Browser album selections persist across server pages and can hand off 1–50 distinct stable local album IDs to shared traversal, batch-download, and export flows. Traversal resolves the ordered local selection into one durable `album_sync` job, checkpoints each album, and optionally creates exactly one article-download handoff after every traversal commits; browser/API/TUI/runtime/browser-client/E2E coverage verifies the bounded opaque-ID path. Export freezes a server-resolved, stable-order, cross-album-deduplicated selection of at most 500 articles before the persistent job is queued. This broad checkbox remains open only for the remaining full browser workflow parity evidence.
- [ ] 5.5 Implement persistent job dashboard, item/log/lease detail, refresh/event feedback, and permitted pause/resume/retry/cancel controls. Implemented actions hand off the created persistent job to its stable workspace detail route; the dashboard exposes bounded job/item/log/lease data and renders only the shared engine's permitted actions. This broad item remains open pending the remaining observation and workflow coverage.
- [ ] 5.6 Implement every export format/options flow, default/custom permitted output selection, manifest/verification, and open-output interaction. Implemented browser export controls validate format-specific options (including HTML resource policy and batch-archive name) against the application allowlist; opaque artifact download streaming and exact-confirmed output opening are implemented. The every-format/options parity item remains open.
- [ ] 5.7 Implement credentials, proxies, preferences, storage backup/restore/integrity/GC, diagnostics, and diagnostic bundle flows. Real maintenance wiring, Credential JSON upload, backup create/verify plus one-shot ZIP download, single-archive restore upload/staging/prepare/commit, integrity, GC plan/apply, diagnostics, and opaque diagnostic-bundle download are implemented; this combined item remains open pending the remaining parity flows.
- [x] 5.8 Add keyboard navigation, focus management, labelled controls, live status announcements, destructive confirmation UX, and narrow-window coverage. Astryx controls receive explicit labels throughout the workspace; `Workspace.tsx` connects skip navigation to the semantic main landmark, announces connection/loading status, and moves focus to each route heading. `e2e/accessibility.spec.ts` verifies keyboard login flow/status, skip/main focus, SPA navigation focus, typed destructive confirmations with focus restoration, and a 390px-wide export workspace without horizontal page overflow. The checked controls also retain visible focus styling in `src/styles/index.css`.

## 6. Verification, documentation, and release

- [ ] 6.1 Add Go handler/integration tests for all API resources, authorization, validation, file boundaries, job transitions, redaction, and graceful shutdown.
- [ ] 6.2 Add browser E2E tests with sanitized loopback fixtures for login UI, account/article selection, job observation/control, export, settings, storage, and failure states. Sanitized Playwright coverage now includes account/article/album selections, discovery plus synchronization mode, typed confirmations, job handoff/control, export options, bounded multi-album export handoff across server pages, client rejection of 51 album IDs, bounded article detail/actions, and stored comment list plus keyboard reply expansion/pending state; handler and library tests cover bounded deterministic stored comment/reply pagination and safe projections. An additional Playwright lane builds and launches the real Go `web --no-open` server, proves session bootstrap/token removal, embedded SPA/deep-route delivery, security headers, preference mutation, and loopback-only requests. This broad item remains open pending every listed workflow and failure state.
- [x] 6.3 Run full `go test ./...`, race, vet, staticcheck, front-end lint/typecheck/test, embedded-asset integrity, and release-size checks in CI. [Browser workspace release verification](../../../docs/release/browser-release-verification.md) maps every required check to the exact `release-cli.yml` job, step, command, and trigger scope; it also records the supplementary strict OpenSpec and sanitized/real-Go Playwright evidence. This verifies the stated CI check classes only; broader browser-workflow parity items remain independently open.
- [x] 6.4 Extend clean-room release receipt workflow with loopback browser workspace launch, security headers, no-retired-domain observation, and representative browser operation evidence.
- [x] 6.5 Document installation, `wechat-article web`, loopback-only privacy model, profile sharing, directory/upload behavior, troubleshooting, accessibility, and language switching.
- [x] 6.6 Publish a capability matrix showing browser parity with Cobra/TUI/MCP; do not mark the browser workspace complete until every required workflow is verified.
